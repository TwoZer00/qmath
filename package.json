{
  "name": "qmath-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "app": "npm start --prefix app",
    "server": "npm run dev --prefix server",
    "dev": "concurrently \"npm run app\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
